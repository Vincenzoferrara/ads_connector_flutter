name: ads_connector
description: Ads connector package.
version: 0.0.2
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  url_launcher: ^6.2.1
  flutter_secure_storage: ^9.0.0
  flutter_meta_sdk: ^1.0.5
  dio_smart_retry: ^6.0.0
  pretty_dio_logger: ^1.3.1
  tiktok_events_sdk: ^0.0.3
  flutter_facebook_auth: ^7.1.1
  app_links: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
